{
  "globs": ["**/*.md"],
  "ignores": ["node_modules/**", "dist/**", "storybook-static/**", ".astro/**", ".wrangler/**"],
  "config": {
    "default": true,
    // 行長制限は無効化(日本語文章・テーブルで実用的でないため)
    "MD013": false,
    // Astro/HTML 混在ドキュメントを許容
    "MD033": false,
  },
}

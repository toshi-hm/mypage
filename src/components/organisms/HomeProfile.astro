---
import ExternalLink from "../atoms/ExternalLink.astro";
---

<section class="home-profile" aria-labelledby="home-profile-heading">
  <div class="profile-photo-wrap">
    <div class="profile-photo-frame">
      <img
        src="/images/profile.webp"
        alt="濱俊也のプロフィール写真"
        width="1200"
        height="1200"
        loading="eager"
        fetchpriority="high"
        decoding="async"
      />
    </div>
    <p class="profile-photo-caption mono">PROFILE / 2026</p>
  </div>

  <div class="profile-copy">
    <p class="eyebrow">About me</p>
    <h2 id="home-profile-heading"><span>濱 俊也</span><small>Hama Toshiya</small></h2>
    <div class="profile-description">
      <p>
        2024年にLINEヤフー株式会社に新卒入社。フロントエンドエンジニアとして、Vue.js / Nuxt、React /
        Next.js
        を用いたWebページの開発や、Express.js、Honoなどを用いたBFF・APIの開発を行っています。
      </p>
      <p>
        近年はビジネス要件をプロダクトの仕様に落とし込む上流工程にも携わり、要件定義から保守運用まで一気通貫でプロダクト開発に取り組んでいます。
      </p>
      <p>
        また、新卒入社と同時に大学院に入学し、人工知能科学を専攻。2026年3月に修了し、修士（人工知能科学）を取得しました。生成AIを理論的に学びつつ、業務でも実用的に活用しています。
      </p>
    </div>
    <a class="profile-link" href="/about/"
      >プロフィール・スキル・経歴を見る <span aria-hidden="true">↗</span></a
    >
    <nav class="profile-social-links" aria-label="プロフィールのソーシャルリンク">
      <ExternalLink href="https://github.com/toshi-hm" label="GitHub">
        <span class="mono">GitHub</span><span aria-hidden="true">↗</span>
      </ExternalLink>
      <ExternalLink href="https://www.linkedin.com/in/toshiyahama" label="LinkedIn">
        <span class="mono">LinkedIn</span><span aria-hidden="true">↗</span>
      </ExternalLink>
    </nav>
  </div>
</section>

<style>
  .home-profile {
    position: relative;
    display: grid;
    grid-template-columns: minmax(14rem, 0.7fr) minmax(0, 1.3fr);
    gap: clamp(2rem, 7vw, 7rem);
    align-items: center;
    margin: 0 0 clamp(5rem, 10vw, 9rem);
    padding: clamp(1.25rem, 3vw, 2.5rem);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-lg) var(--radius-lg) var(--radius-sm) var(--radius-lg);
    background: color-mix(in srgb, var(--color-surface) 76%, transparent);
    box-shadow: var(--shadow-sm);
    isolation: isolate;
  }
  .home-profile::before {
    content: "";
    position: absolute;
    z-index: -1;
    inset: 1.25rem auto auto 1.25rem;
    width: min(24vw, 17rem);
    aspect-ratio: 1;
    border-radius: 50%;
    background: var(--color-highlight-soft);
    transform: translate(-1.5rem, -1.5rem);
  }
  .profile-photo-wrap {
    position: relative;
    max-width: 22rem;
    margin: 0 auto;
  }
  .profile-photo-frame {
    position: relative;
    overflow: hidden;
    aspect-ratio: 1;
    border: 0.55rem solid var(--color-surface);
    border-radius: 48% 52% 45% 55% / 46% 44% 56% 54%;
    box-shadow: var(--shadow-lg);
    transform: rotate(-2deg);
  }
  .profile-photo-frame::after {
    content: "";
    position: absolute;
    inset: 0;
    border: 1px solid rgb(255 255 255 / 45%);
    border-radius: inherit;
    pointer-events: none;
  }
  .profile-photo-frame img {
    display: block;
    width: 100%;
    height: 100%;
    object-fit: cover;
  }
  .profile-photo-caption {
    position: absolute;
    right: -0.5rem;
    bottom: -1.1rem;
    margin: 0;
    padding: 0.45rem 0.7rem;
    border: 1px solid var(--color-border);
    border-radius: 999px;
    background: var(--color-surface);
    color: var(--color-text-muted);
    font-size: 0.62rem;
    font-weight: 800;
    letter-spacing: 0.1em;
    box-shadow: var(--shadow-sm);
    transform: rotate(3deg);
  }
  .profile-copy {
    min-width: 0;
  }
  .profile-copy h2 {
    display: flex;
    flex-wrap: wrap;
    align-items: baseline;
    gap: 0.5rem 1rem;
    margin: 0;
    font-size: clamp(2rem, 4vw, 3.6rem);
  }
  .profile-copy h2 span {
    color: var(--color-accent);
  }
  .profile-copy h2 small {
    color: var(--color-text-muted);
    font: 700 clamp(0.85rem, 1.5vw, 1.15rem)/1 var(--font-mono);
    letter-spacing: 0;
  }
  .profile-description {
    margin: 1.5rem 0 1.75rem;
    color: var(--color-text-muted);
  }
  .profile-description p {
    margin: 0;
  }
  .profile-description p + p {
    margin-top: 0.85rem;
  }
  .profile-link {
    display: inline-flex;
    align-items: center;
    gap: 0.65rem;
    color: var(--color-text);
    font-weight: 750;
    text-decoration: none;
  }
  .profile-link span {
    display: grid;
    place-items: center;
    width: 1.75rem;
    aspect-ratio: 1;
    border-radius: 50%;
    background: var(--color-highlight);
    color: var(--color-on-highlight);
    transition: transform 180ms ease;
  }
  .profile-link:hover span {
    transform: translate(2px, -2px);
  }
  .profile-social-links {
    display: flex;
    flex-wrap: wrap;
    gap: 0.6rem;
    margin-top: 1.5rem;
  }
  .profile-social-links :global(.external-link) {
    display: inline-flex;
    align-items: center;
    gap: 0.65rem;
    min-height: 2.35rem;
    padding: 0.35rem 0.45rem 0.35rem 0.75rem;
    border: 1px solid var(--color-border);
    border-radius: 999px;
    background: var(--color-surface);
    color: var(--color-text);
    font-size: 0.72rem;
    font-weight: 750;
    text-decoration: none;
    box-shadow: var(--shadow-sm);
  }
  .profile-social-links :global(.external-link) span:last-child {
    display: grid;
    place-items: center;
    width: 1.45rem;
    aspect-ratio: 1;
    border-radius: 50%;
    background: var(--color-highlight);
    color: var(--color-on-highlight);
  }
  .profile-social-links :global(.external-link:hover) {
    border-color: var(--color-accent);
    color: var(--color-accent);
    transform: translateY(-2px);
  }
  @media (max-width: 700px) {
    .home-profile {
      grid-template-columns: 1fr;
      gap: 2.5rem;
    }
    .profile-photo-wrap {
      width: min(70vw, 19rem);
    }
  }
  @media (prefers-reduced-motion: reduce) {
    .profile-link span {
      transition: none;
    }
  }
</style>
